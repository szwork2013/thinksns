/**
 * ThinkSNS 配置文件
 */

// 网址根地址
const base = '//demo.thinksns.com/ts4';

// Root URL.
export const base_url = base;

// Dist URL.
export const dist_url = base + '/storage/app/h5/';

// H5 api build URL.
export const h5_api_url = base + '/index.php?app=h5&mod=%controller%&act=%action%';

// API URL.
export const api_url = base + '/api.php?mod=%controller%&act=%action%';

// Website title.
export const title = 'ThinkSNS';

// Website description.
export const description = 'ThinkSNS web application.';

// Website keywords.
export const keywords = 'ts,t4,thinksns';

